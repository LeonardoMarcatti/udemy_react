import React from "react";
import styles from './Footer.module.css'

const Footer = () => {
   return <footer className={styles.footer}>
      <small>Feito com React Router</small>
   </footer>
}

export default Footer