import Counter from './components/Counter'
const  App = () => {
  // const auth = useSelector(state => state.auth.isAuthenticated)
  return (
    <>
      <Counter />
    </>
  );
}

export default App;
